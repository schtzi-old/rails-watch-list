<div class="card-category"
  <% if list.file.attached? %>
    style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path list.file.key, height: 180, width: 300, crop: :fill %>)">
  <% end %>
  <%= list.name %>
</div>
